# === MONITORES ===
# Este arquivo é GERADO AUTOMATICAMENTE pelo script:
#   ~/.config/hypr/scripts/auto-config-monitors.sh
#
# NÃO EDITE MANUALMENTE — suas mudanças serão sobrescritas no próximo boot
#
# Pra forçar regeneração agora, rode:
#   ~/.config/hypr/scripts/auto-config-monitors.sh
#
# Config padrão (placeholder até o script rodar):
monitor = , preferred, auto, 1
