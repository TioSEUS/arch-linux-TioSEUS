# === VARIÁVEIS DE AMBIENTE ===
# Baseado na wiki oficial do Hyprland
# https://wiki.hypr.land/Configuring/Environment-variables

# --- TOOLKIT BACKEND ---
env = GDK_BACKEND,wayland,x11,*
env = QT_QPA_PLATFORM,wayland;xcb
env = SDL_VIDEODRIVER,wayland
env = CLUTTER_BACKEND,wayland

# --- XDG ---
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = XDG_MENU_PREFIX,plasma-

# --- THEMING & RENDERING (Estilo Maciejonos para Nautilus GTK4) ---
env = GTK_THEME,Adwaita-dark
env = ADW_DISABLE_PORTAL,0
env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1

# --- CURSOR ---
env = XCURSOR_SIZE,24
env = HYCURSOR_SIZE,24

# --- XWAYLAND ---
env = ELECTRON_OZONE_PLATFORM_HINT,wayland

# === OTIMIZAÇÃO PARA JOGOS ===
env = __GL_GSYNC_ALLOWED,1
env = __GL_VRR_ALLOWED,1

# === NVIDIA (Auto-gerenciado pelo seu setup.sh) ===
# env = GBM_BACKEND,nvidia-drm
# env = __GLX_VENDOR_LIBRARY_NAME,nvidia
# env = LIBVA_DRIVER_NAME,nvidia
# env = NVD_BACKEND,direct

# === AQUAMARINE (Evita flicker em multi-GPU) ===
# env = AQ_NO_MODIFIERS,1
